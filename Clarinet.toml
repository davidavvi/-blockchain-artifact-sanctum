[project]
name = "blockchain-artifact-sanctum"
authors = []
description = ""
telemetry = true
requirements = []
cache_dir = "/home/runner/workspace/blockchain-artifact-sanctum/./.requirements"
boot_contracts = ["pox", "costs-v2", "bns"]
[contracts.blockchain-artifact-sanctum]
path = "contracts/blockchain-artifact-sanctum.clar"

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
